

git status
git add <filename>      -- adds file to be tracked by git
git commit -m 'message' -- adds file with changes to local repository
git push                -- pushes committed changes to team directory
    --set-upstream origin <branch-name> for new branches
	
git checkout -b <branch-name> -- creates new branch 
git checkout <branch-name>    -- switches to exiting branch